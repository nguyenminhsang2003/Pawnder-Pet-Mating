**Bộ setup “đủ dùng” cho vai trò Team Lead**,  
**không overkill**, **team làm theo được ngay**, **review & merge gọn**.

Áp dụng trực tiếp cho repo lớn (như `Pawnder`) và có thể dùng lại cho các dự án khác.

---

## 🎯 Mục tiêu

- **Dev**: Biết rõ phải làm gì, tạo PR/issue không bị “tùy hứng”.
- **PR**: Có đủ bối cảnh, dễ review, dễ trace sau này.
- **Lead**: Kiểm soát được merge, không phải nhắc miệng nhiều.

---

## ✅ Những thứ BẮT BUỘC PHẢI CÓ

### 1️⃣ Pull Request Template

📁 File:

```text
.github/pull_request_template.md
```

**Mục đích**

- Ép dev **mô tả rõ**: làm gì, tại sao làm.
- Giúp reviewer **nắm ngữ cảnh nhanh**, không phải tự mò code.
- Chuẩn hóa cách báo cáo test & ảnh hưởng side-effect.

**Gợi ý cấu trúc template**

- **Title**: `[BE]` / `[FE-User]` / `[FE-Admin]` + nội dung ngắn gọn.  
- **Mô tả**: Tóm tắt 2–5 dòng, link issue (nếu có).  
- **Thay đổi chính**: Bullet theo từng nhóm (API, UI, DB, logic…).  
- **Đã test gì?**: Liệt kê các bước test thủ công + link test tự động (nếu có).  
- **Checklist**:  
  - [ ] Đã tự review lại code  
  - [ ] Không quên cập nhật README/Docs (nếu cần)  
  - [ ] Không quên cập nhật migration/SQL (nếu cần)  

➡️ Đây là thứ **bắt buộc** để mọi PR đều có “form” tối thiểu.

---

### 2️⃣ Branch Protection cho `main` và `develop`

Áp dụng trong GitHub → **Settings → Branches → Branch protection rules**.

**Bắt buộc cho 2 nhánh**

```text
main
develop
```

**Rule nên bật**

- ✅ Require pull request before merging  
- ✅ Require approvals: **tối thiểu 1**  
- ✅ Require review from Code Owners  
- ✅ Restrict who can push trực tiếp:
  - Chỉ lead / bot CI (nếu có), **không cho dev push thẳng**.

**Kết quả**

- Dev **không thể merge trực tiếp** vào `main` / `develop`.  
- Mọi thay đổi đều phải qua **PR + review**.  
- Lead (hoặc người được chỉ định) là người **quyết định merge**.

➡️ Đây là “đòn bẩy quyền lực” quan trọng nhất của Team Lead.

---

### 3️⃣ CODEOWNERS

📁 File:

```text
.github/CODEOWNERS
```

**Ví dụ đơn giản (team nhỏ)**

```text
* @your-github-username
```

Hoặc chi tiết hơn:

```text
/BackEnd/   @your-github-username
/FE/FE-User/ @your-github-username
/FE/fe-admin/ @your-github-username
```

**Ý nghĩa**

- Bất kỳ PR nào chạm vào code được khai trong `CODEOWNERS` sẽ:
  - Tự động **request review** từ người đó.
  - Có thể bắt buộc **code owner approve** trước khi merge (kết hợp Branch Protection).

➡️ Đảm bảo mọi thay đổi quan trọng **phải qua tay bạn** (hoặc người lead tương ứng).

---

## ⭐ Những thứ NÊN CÓ (rất hữu ích)

### 4️⃣ Issue Template / Issue Form

📁 Folder:

```text
.github/ISSUE_TEMPLATE/
```

**Tối thiểu nên có 2 loại**

- 🐞 `bug_report.md` – mô tả bug.  
- 🚀 `feature_request.md` – đề xuất tính năng mới.

**Lợi ích**

- Task/bug **rõ ràng từ đầu**, dev không ghi note kiểu “fix cái này giúp anh”.  
- Sau này dễ trace: **ai yêu cầu, bối cảnh gì, đã làm gì**.  
- Dùng làm input để planning/sprint hoặc review scope đồ án.

⚠️ Không cần bắt buộc issue cho từng việc nhỏ, nhưng **task lớn / feature / bug quan trọng** thì nên luôn đi kèm issue.

---

### 5️⃣ Quy ước branch + commit (ghi trong `CONTRIBUTING.md`)

📁 File:

```text
CONTRIBUTING.md
```

**Nội dung nên có**

- Cách đặt tên branch:

  ```text
  feature/pet-matching-api
  feature/mobile-chat-ui
  bugfix/appointment-timezone
  hotfix/payment-callback
  ```

- Cách viết commit message (ví dụ, tối giản):

  ```text
  feat: add pet matching API
  fix: correct appointment timezone handling
  refactor: cleanup chat service
  docs: update backend README for setup
  ```

- Luồng làm việc:
  - Tạo branch từ `develop`.  
  - Làm việc → commit nhỏ, có ý nghĩa.  
  - Mở PR → gắn issue (nếu có).  
  - Review → chỉnh sửa → squash/rebase (nếu quy định).  
  - Lead/owner merge vào `develop` / `main`.

➡️ Không cần enforce bằng tool/script ngay từ đầu.  
➡️ **Chỉ cần viết rõ ra là đã đạt ~80% hiệu quả quản lý**.

---

### 6️⃣ README cho người mới (root + từng module)

📁 Các file:

- `README.md` (root) – overview, kiến trúc, cách run tổng thể.  
- `BackEnd/README.md` – backend API, migration, background jobs.  
- `FE/FE-User/README.md` – mobile app.  
- `FE/fe-admin/README.md` – admin panel.  
- `database/README.md` – PostgreSQL, import, backup/restore.

**Nên đảm bảo luôn có**

- Cách run project (dev): lệnh, env, port.  
- Tech stack chính.  
- Luồng branch & deploy (tối giản, có thể vẽ ASCII).

**Mục tiêu**

> Người mới vào repo **chỉ cần đọc README là tự chạy được project**,  
> không phải hỏi lead 10 câu đầu tiên.

---

## ❌ Những thứ TẠM THỜI CHƯA CẦN (tránh overkill)

- ❌ Form ép tạo commit message phức tạp.  
- ❌ CI/CD pipeline quá nặng nếu team còn nhỏ / đồ án.  
- ❌ Script kiểm tra tên branch tự động.  
- ❌ Workflow quá cứng, làm chậm dev.

➡️ Khi team lớn hơn, repo sống lâu hơn, có thể nâng cấp dần:
- Thêm CI (lint/test/build) bắt buộc trước merge.  
- Thêm rule check branch/commit.  
- Thêm auto-deploy cho main/develop.

---

## 🧠 Bảng tóm tắt nhanh

| Hạng mục              | Trạng thái gợi ý |
| --------------------- | ---------------- |
| PR template           | ✅ BẮT BUỘC       |
| Branch protection     | ✅ BẮT BUỘC       |
| CODEOWNERS            | ✅ BẮT BUỘC       |
| Issue template        | ✅ NÊN            |
| CONTRIBUTING          | ✅ NÊN            |
| README chi tiết       | ✅ NÊN (luôn cập nhật) |
| CI phức tạp           | ❌ CHƯA CẦN       |

---

